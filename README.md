# 기상청 해상 기상특보 자료 수집 자동화

기상청 전자민원(minwon.kma.go.kr)의 기상현상증명에서 **해상 기상특보(태풍·풍랑)**
과거 자료를 연도별로 자동 신청하고 PDF까지 내려받는 Playwright 스크립트 모음.

한 번에 신청 가능한 용량 제한 때문에 구역을 나눠 수십 번 반복해야 하는데,
이 반복(구역 체크 → 기간 입력 → 신청 → 발급 PDF 저장)을 자동화했다.

---

## 스크립트 구성

| 파일 | 용도 | 연도당 작업 수 |
|---|---|---|
| `kma_minwon_auto.py` | 2022년 이후 (현행 구역명, 1년) | 30건 |
| `kma_minwon_auto_pre2021.py` | 2020년 이하 (옛 ~'21.07.29 구역명, 1년) | 30건 |
| `kma_minwon_auto_2021_상반기.py` | 2021년 먼바다 1.1~7.29 보완 | 12건 |
| `kma_minwon_auto_2019_상반기.py` | 2019년 동해남부·서해남부 먼바다 1.1~4.30 보완 | 4건 |
| `rename_files.py` | 내려받은 PDF 파일명 일괄 정리 (유틸) | - |

신청 스크립트 4개는 실행 방법·동작이 모두 같고, 대상 구역·기간·파일명 꼬리표만 다르다.
연도별로 어떤 스크립트를 쓰는지는 아래 "연도별 사용법" 참고.

---

## 사전 준비

```bash
# 파이썬 환경 (conda 예시)
conda create -n kma python=3.11 -y
conda activate kma
pip install playwright
```

> Playwright 전용 브라우저 설치(`playwright install`)는 **불필요**.
> 이미 열려 있는 엣지(Edge)에 접속하는 방식이라서다.

---

## 실행 방법

### 1. 엣지를 디버깅 모드로 실행 + 로그인

기존 엣지 창을 모두 닫고, 새 명령 프롬프트에서:

```bash
"C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222 --user-data-dir="C:\edge_automation"
```

뜬 엣지 창에서 전자민원에 **PASS 로그인**을 마친다. 이 창은 작업이 끝날 때까지
닫지도 조작하지도 않는다. (로그인은 세션당 1회만 사람이 하면 됨)

### 2. 스크립트 설정 (파일 상단)

| 설정 | 의미 |
|---|---|
| `MODE` | `"dump"` / `"dry"` / `"run"` (아래 참조) |
| `YEARS` | 처리할 연도. 예: `[2023]` |
| `DOWNLOAD_ROOT` | PDF 저장 루트 폴더 |
| `BOARD_URL` | 민원보관함 주소 (나의민원 > 민원보관함 진입 후 주소창 URL) |
| `DELAY_SEC` | 신청 간 대기(기본 4초, 줄이지 말 것) |

**MODE 3단계**
- `dump` : 사이트의 구역 체크박스 이름을 출력만 함 (ZONE_GROUPS 검수용)
- `dry`  : 신청 확인 페이지까지 리허설, 실제 신청·다운로드 안 함
- `run`  : 실제 신청 + 신청 직후 PDF 자동 저장

### 3. 실행

```bash
conda activate kma
cd /d C:\kma_auto
python kma_minwon_auto.py
```

**처음 돌리는 연도라면** `dump`(필요 시) → `dry` 전체 통과 확인 →
1건만 `run` 테스트 → 전체 `run` 순서를 권장.

---

## 연도별 사용법

먼바다 구역명이 아래 날짜에 개편되어, **개편일이 걸친 연도**는 기간을 쪼개
두 번(개편 전/후) 신청해야 1년이 완성된다. 앞바다는 개편 영향이 없다.

| 개편일 | 내용 | 걸치는 연도 |
|---|---|---|
| ~'14.12.10 | 남해서부먼바다 → 서/동 분할 | 2014 |
| ~'19.04.30 | 동해남부·서해남부 먼바다 → 북/남 분할 | 2019 |
| ~'21.07.29 | 대부분 먼바다 → 안쪽/바깥 분할(현행) | 2021 |

| 연도 | 실행할 스크립트 |
|---|---|
| 2022~현재 | `kma_minwon_auto.py` |
| 2021 | `kma_minwon_auto.py`(하반기) + `kma_minwon_auto_2021_상반기.py`(상반기) |
| 2020 | `kma_minwon_auto_pre2021.py` (YEARS=[2020]) |
| 2019 | `kma_minwon_auto_pre2021.py`(YEARS=[2019]) + `kma_minwon_auto_2019_상반기.py` |
| 2015~2018 | `kma_minwon_auto_pre2021.py` (구역명 확인 후) |

> **원리**: 현행 구역명 + 1년 → 개편일 이후 자료만 나옴 / 옛 구역명 + 1년 →
> 개편일 이전 자료만 나옴 / 둘을 한 신청에 합치면 용량 초과. 그래서 개편 연도는
> 개편일 앞뒤로 나눠 두 번 받는다.

---

## 저장 규칙

```
{DOWNLOAD_ROOT}\{연도}\{해역}\{앞바다|먼바다}\기상특보(해상)_{연도}_{해역}_{구분}_{특보}[_기간꼬리표].pdf
```

- 폴더명은 `(1)(2)` 꼬리를 뗀 통일형(예: `앞바다`), 파일명엔 구분 원본 유지
  (서해남부 앞바다(1)(2) 파일이 같은 `앞바다` 폴더에 함께 저장됨)
- 기간 꼬리표는 개편 연도만: 2021 `_0101-0729`/`_0730-1231`,
  2019(동해남부·서해남부 먼바다만) `_0101-0430`/`_0501-1231`
- 신청 이력은 `신청로그.csv`에 자동 기록

예:
```
2024\동해중부\앞바다\기상특보(해상)_2024_동해중부_앞바다_태풍주의보.pdf
2021\동해중부\먼바다\기상특보(해상)_2021_동해중부_먼바다_풍랑주의보_0101-0729.pdf
```

### 이미 받은 파일 이름 정리 — `rename_files.py`

폴더 경로(`연도\해역\구분\`)에서 해역·구분을 읽어 파일명을 새 규칙으로 바꾼다.
`DRY_RUN = True`로 미리보기 후 `False`로 실제 변경.

---

## 동작 원리 (요약)

1. 민원보관함 맨 위 신청번호를 기억
2. 신청 페이지에서 학술/연구 · 기상특보>해상 · 구역 · 특보종류 · 기간 자동 입력
   (구역 목록이 비동기로 다시 그려져 체크가 풀리는 문제 → 체크 유지 검증·재시도)
3. 확인 페이지에서 특보·연도·구역을 대조(불일치 시 신청 안 함) 후 신청
4. 보관함에 뜬 새 신청건의 발급 파라미터로 PDF URL을 만들어 세션 쿠키로 직접 GET,
   응답 본문(PDF)을 규칙대로 저장

---

## 주의사항

- 발급은 신청 후 7일까지만 가능 → 신청한 연도는 바로 다운로드까지 완료할 것
  (스크립트가 신청 직후 자동 저장하므로 보통 문제없음)
- 정부 사이트 세션은 약 30분 후 만료될 수 있음 → 연도 단위로 나눠 실행 권장,
  중간에 전부 실패하면 엣지 창에서 PASS 재로그인 후 실패분만 재실행
- 신청 간 간격(기본 4초)은 서버 부하 방지용 → 줄이지 말 것
- 사이트가 개편되면 `dump` 모드 + 브라우저 F12로 셀렉터부터 재확인

---

## 참고

본인 계정으로 본인이 열람 가능한 자료를 신청하는 정상 이용 범위의 자동화이며,
과도한 요청을 피하기 위해 신청 사이에 지연을 둔다.

---
## 노션 링크

해당 작업 수행 및 정리 내용은 다음 노션 링크에 저장해두어 관리했다.

https://app.notion.com/p/3a44729efa2a8001afa2df3a32e4526c?source=copy_link
